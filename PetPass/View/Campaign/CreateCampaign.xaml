<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PetPass.View.Campaign.CreateCampaign"
             Title="CreateCampaign">
    <StackLayout Padding="15">
        <Label Text="Nombre de la Campaña" />
        <Entry Placeholder="Ingrese el nombre de la campaña" Text="{Binding CampaignName}" />

        <Label Text="Fecha de Inicio" />
        <DatePicker Date="{Binding StartDate}" />

        <Label Text="Fecha de Finalización" />
        <DatePicker Date="{Binding EndDate}" />

        <Button Text="Guardar Campaña" Command="{Binding SaveCampaignCommand}" />
    </StackLayout>
</ContentPage>